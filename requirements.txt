# TaxoConf – LLM-Taxonomy Conference Session Organizer
# Core
fastapi>=0.104.0
uvicorn>=0.24.0
pyyaml>=6.0

# ML / similarity
scikit-learn>=1.3.0
numpy>=1.24.0
networkx>=3.1

# LLM providers (install the ones you need)
openai>=1.12.0            # OpenAI + xAI (Grok)
google-genai>=1.0.0       # Google Gemini
anthropic>=0.25.0         # Anthropic Claude

# Optional: sentence-transformer embeddings (uses TF-IDF by default)
# sentence-transformers>=2.2.0
